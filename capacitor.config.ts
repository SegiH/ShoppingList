import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.segihovav.shoppinglist',
  appName: 'Shopping List',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
